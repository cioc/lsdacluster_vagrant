Lsdacluster environment via vagrant (vagrantup.com).

Installation
============

Starting the VM
===============

```shell
vagrant up
```

Starting the Notebook Server
============================

```shell
vagrant ssh
ipython notebook --ip='*'
```
