Lsdacluster environment via vagrant (vagrantup.com).

Installation
============

Starting the VM
===============

```shell
vagrant up
```

Starting the Notebook Server
============================

```
vagrant ssh
ipython notebook --ip='*'
```shell

To start the notebook:

```
